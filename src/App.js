import Session from './sessions/useRef/UseRef';

const App = () => {
  return (
    <div className='container'>
      <Session />
    </div>
  );
};

export default App;

// React functional components uses a concept call HOOK to update the content in DOM

//Will help us to control component life cycle

//useState - update data in a stateful component

//
